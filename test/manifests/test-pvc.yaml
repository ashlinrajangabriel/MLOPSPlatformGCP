apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: developer-platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gcsfuse-versioned
  resources:
    requests:
      storage: 10Gi 